syntax = "proto3";

package models;

option go_package = "pkg/grpc/models";

message KioskState {
	string url  = 1;
	string title = 2;
	int64 size_w = 3;
	int64 size_h = 4;

	EnumState state = 5;
}

enum EnumState {
    RUNNING = 0;
	STOPPED = 1;
	UNKNOWN = 2;
}